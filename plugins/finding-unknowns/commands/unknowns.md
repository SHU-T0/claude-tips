---
description: Start a map-unknowns walk on a task before writing any code.
argument-hint: [task description]
---

Map the unknowns for this task before writing any code:

$ARGUMENTS

Follow the **map-unknowns** skill: first disclose how familiar each side is with
the codebase and domain, then walk the four quadrants in order (known knowns →
known unknowns → unknown knowns → unknown unknowns), react over imagine, run a
blind-spot pass (dispatch the **blind-spot-scout** subagent), and end with a
written `unknowns-map.md` plus a volatile-first implementation plan. Do not start
implementing — hand over the map.
